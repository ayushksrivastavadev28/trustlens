{
  "name": "@trustlens/ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -c \"import os,uvicorn; uvicorn.run('app.main:app', host='0.0.0.0', port=int(os.getenv('PORT','8001')), reload=True)\"",
    "start": "python -c \"import os,uvicorn; uvicorn.run('app.main:app', host='0.0.0.0', port=int(os.getenv('PORT','8001')))\""
  }
}
